extends: conditional
message: "At least one '%s' must be marked as ™*"
level: error
scope: text
ignorecase: false

# * `Redis`
# * `Redis™`
# * `Redis™*`.
# * `Redis™* and more` -- this should "produce" `Redis™*``
# * `Redis™*?`         -- this should "produce" `Redis™*``
# * `Redis™*bibble`    -- strictly optional, but probably good if it can

##first: '\b(Redis)(?:[™\*]*\W)'
first: '\b(Redis)'
second: '\b(Redis)(?:™\*\W)'
