name: KafkaConnector - Create PR to Update Advanced parameters

on:
  schedule:
    - cron: "0 6 * * 2"

jobs:
  AdvancedParamsKafkaConnector:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the repo
        uses: actions/checkout@v2

      - name: Set up Python 3.8
        uses: actions/setup-python@v2
        with:
          python-version: "3.8"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: KafkaConnector - Update Advanced parameters
        run: make service-type-config-kafka_connect

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v4
        with:
          commit-message: KafkaConnector - Update Advanced parameters
          title: KafkaConnector - Update Advanced parameters
          body: KafkaConnector - update advanced parameters file
          base: main
          branch: kafka-connector-update-advanced-params
          labels: kafka, automated-pr
          delete-branch: true
