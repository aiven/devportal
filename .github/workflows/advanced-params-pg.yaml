name: Create PR of generated auto docs

on:
  push:
  pull_request:
  schedule:
    - cron: "* * * * *"

jobs:
  AdvancedParamsPostgreSQL:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the repo
        uses: actions/checkout@v2

      - name: Set up Python 3.8
        uses: actions/setup-python@v2
        with:
          python-version: "3.8"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: PostgreSQL - update advanced params
        run: make service-type-config-pg

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v4
        with:
          commit-message: PostgreSQL - update advanced params
          title: PostgreSQL - update advanced params
          body: PostgreSQL - update advanced parameters file
          base: main
          branch: pg-update-advanced-params
          labels: pg, automated-pr
          delete-branch: true
