name: InfluxDB - Create PR to update advanced params

on:
  schedule:
    - cron: "0 6 * * 2"

jobs:
  AdvancedParamsInfluxDB:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the repo
        uses: actions/checkout@v2

      - name: Set up Python 3.8
        uses: actions/setup-python@v2
        with:
          python-version: "3.8"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: InfluxDB - update advanced params
        run: make service-type-config-influxdb

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v4
        with:
          commit-message: InfluxDB - update advanced params
          title: InfluxDB - update advanced params
          body: InfluxDB - update advanced parameters file
          base: main
          branch: influxdb-update-advanced-params
          labels: influx, automated-pr
          delete-branch: true
